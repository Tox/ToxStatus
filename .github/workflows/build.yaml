name: build
on: [pull_request, push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        uses: actions/setup-go@v3
        with:
          go-version: '^1.16'
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: make
